// ============================================================
//  BOOTSTRAP
// ============================================================
const agent = new AIAgent();
agent.init().catch(err => console.error('Agent init failed:', err));